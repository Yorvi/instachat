
<% if ( current_user && current_user.admin ) || (current_user && current_user.id == post.user_id ) %>
  <%= link_to "Edit", edit_post_path(post) %> // 
  <%= link_to "Destroy", post, method: :delete, data: { confirm: "Are you sure you want to delete this post?" } %>
<% end %>